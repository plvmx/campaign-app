# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Git workflow

### Autonomous (no approval needed)
- Create feature branches (`git checkout -b <branch>`)
- Stage and commit changes — always with a clear message summarising *why*
- Push feature branches to origin (`git push origin <feature-branch>`)
- Create PRs (`gh pr create`) — include summary, test plan, and link to any related task
- Check CI status (`gh run list`, `gh run view`)
- Fix CI failures and push follow-up commits to the same branch

### Always pause and wait for explicit "go ahead"
- **Merging a PR to main** (`gh pr merge`) — main auto-deploys to production
- **Pushing directly to main** — blocked in settings; if ever needed, ask first
- **Force-pushing anything** — blocked in settings; never bypass without discussion

### Communication
- After every commit, state: branch name, commit hash (short), and one-line summary
- After every push, confirm where it went and whether CI was triggered
- When creating a PR, share the URL so Peter can review before approving the merge
- If CI fails, diagnose and fix without asking — then report what was wrong and what changed

## Branch naming
`<type>/<short-description>` — e.g. `fix/campaign-date-filter`, `feat/pagination`, `chore/update-actions`

## Code quality (standing rule)
Every change must pass all four CI jobs: **Lint · Type-check · Unit tests · Build**.  
Never suppress lint errors with disable comments unless there is no correct refactor — always prefer fixing the root cause.

## Testing policy
- Every bug-fix PR includes a regression test that fails on the pre-fix code and passes on the post-fix code, in the same PR. If the bug is purely visual/CSS/copy and genuinely can't be captured in a test, say so explicitly in the PR description rather than silently skipping it.
- Every new function added to `lib/` or `lib/services/` ships with a test in the same PR.
- Before calling a fix "done," verify red→green: run the new/updated test against the pre-fix code (e.g. `git stash`, or check out the parent commit for a specific file) to confirm it actually fails, then against the fix to confirm it passes. A test that currently passes is not sufficient on its own — it must be shown to have caught the original bug.
- Mock the Supabase client with the shared builder in `lib/services/__tests__/supabaseMock.ts` rather than hand-rolling `vi.mock` chains per test file.
- If a production incident is investigated with a one-off script in `scripts/`, the investigation isn't closed until the root cause is captured as a permanent test in `lib/__tests__/` (or `lib/services/__tests__/`) — the script is evidence, not the fix's safety net.
- Role/status checks against `state_leaders.admin` must go through `isRecognizedAdminStatus()` in `lib/campaignFilter.ts` — never re-implement `=== 'AD' || === 'SR'` inline. See #78 (login role bug) for why: a truthy check in one call site silently misrouted leaders with junk data in that column.

## Pre-PR checklist (standing rule)
Before opening a PR, work through this list and note the outcome of each item in the PR description (a plain "N/A" is fine — don't skip silently). This exists because it's easy for backups, docs, and cleanup to quietly fall behind as the app grows.

1. **Backups in sync** — did this change add, remove, or rename a database table, or change a column that a backup depends on? If so, update `BACKUP_TABLE_CONFIG` in `lib/services/backupService.ts` (new tables go in FK-safe order — only after every table they reference), bump the export format `version` per the comment above it, and confirm `/admin/backup` still exports/restores cleanly.
2. **Docs in sync** — did this change add/remove/rename a route, page, service module or exported function, database table, or otherwise touch something described in this file's Architecture section (Page map, Database tables, service tables)? Update `CLAUDE.md` itself in the same PR. If it changes setup steps, env vars, or user-facing behaviour covered in `README.md` or `TECHNICAL_DOCS.md`, update those too.
3. **Quality pass** — run `/code-review` (or `/simplify`) against the diff, specifically looking for redundancy the change introduced: logic that duplicates an existing `lib/services/` function, inline role checks bypassing `isRecognizedAdminStatus()`, repeated/N+1 Supabase calls that could be batched, or dead code left behind by the change. Either fix what's found in this PR, or note it explicitly as a follow-up in the PR description — don't let it pass unmentioned.

## Deployment
- `main` branch → auto-deploys to production via Vercel/hosting
- Do not merge until CI is green and Peter has approved the PR

---

## Commands

```bash
npm run dev        # Start Next.js dev server
npm run build      # Production build
npm run lint       # ESLint across app/, components/, lib/
npx tsc --noEmit   # Type-check without emitting
npm test           # Run all Vitest unit tests (vitest run)
npx vitest run lib/__tests__/auth.test.ts   # Run a single test file
```

---

## Architecture

### Stack
- **Next.js 16 App Router** — all pages live under `app/`. No Pages Router.
- **React 19** with client components (`'use client'`) for all interactive UI.
- **Supabase** (`@supabase/supabase-js`) — Postgres database + auth. Two clients:
  - `lib/supabaseClient.ts` — browser client, used everywhere in the frontend.
  - `lib/supabaseAdmin.ts` — service-role client, used only in API routes.
- **Tailwind CSS v4** — utility-first styling, dark-mode via `dark:` variants.
- **Vitest + jsdom + React Testing Library** — unit tests in `lib/__tests__/`.

### Auth & roles
- Login is mobile-number + name via `lib/auth.ts` (`signInWithMobileAndName`).
- Role is resolved at runtime from the `state_leaders` table:
  - `admin = 'AD'` → full admin
  - `admin = 'SR'` → state reporter (can only see/edit their own state)
  - Otherwise → regular user
- `contexts/UserContext.tsx` (`useUser()`) is the single source of truth for the current user, profile, role (`isAdmin`, `adminStatus`), state, and leader. Pages read from this context rather than querying auth themselves.
- Role checking is done inline: compare `adminStatus === 'AD'` (full admin) or `adminStatus === 'SR'` (state reporter). No permissions module — role logic lives in `UserContext` and `campaignFilter.ts`.
- **Leader email capture (Phases 1-3 of a longer rollout toward real Supabase Auth + MFA for every leader)** — `state_leaders` now carries `email`/`pending_email`/`user_id`/`email_confirmed_at` (`scripts/add_email_to_state_leaders.sql`), but login itself is unchanged so far: mobile+name is still the only active identity check, and `user_id` isn't read by any RLS policy yet. Right after a successful mobile+name sign-in, `/login` shows a skippable step asking a leader with no confirmed `email` to add/confirm one — pre-filled from `pending_email` or a `registry.registrants` phone-match suggestion (`lib/services/registrantEmailSuggestionService.ts`; a suggestion only, never written back to `state_leaders` directly, since that table has no reliable identity key to a specific leader — see `lib/registryPipeline/leaderMatch.ts`'s own comment on why it matches by phone instead). Submitting calls `POST /api/auth/propose-email`, which re-verifies the same mobile+name pair server-side (`lib/services/leaderVerificationService.ts`'s `findVerifiedStateLeaders` — a bare leader id is never trusted) before writing `pending_email` and sending a real Supabase magic link. The magic-link redirect also carries the leader id as a `?leaderId=` query param (set by `propose-email`), which the `/confirm-email` page (`lib/emailConfirmSupabaseClient.ts` — an isolated Supabase client with its own localStorage key, exactly mirroring `lib/registrySupabaseClient.ts`, so detecting/signing-out of this magic-link session can't collide with the leader's real anonymous app session) forwards to `POST /api/auth/confirm-email` alongside the session's access token; that route trusts only `supabaseAdmin.auth.getUser(token)` for the confirmed email (never a client-supplied string) but scopes its commit to that one `leaderId` + matching `pending_email` — never a blanket match on the email string alone, since two different leaders could otherwise propose the same address (a shared household email, or a typo) and whichever confirms first would silently attach their identity to the other's row too. A leader with several `state_leaders` rows — whether across states, or multiple rows within one state (an AD row, an SR row, and/or a plain leader row, differentiated by name/mobile to satisfy the table's own state+leader uniqueness) — confirms each row independently this way and they can all share one address; `email` has no DB-level uniqueness constraint (dropped in `scripts/drop_state_leaders_email_unique_index.sql` after that turned out to block exactly this, a live rollout blocker — see the removal script's own comment for why the app-level leaderId-scoping above is protection enough on its own). Every confirmation is logged to `state_leader_email_changes`. Admins can also set a leader's `email` directly from `/admin/state-leaders` with no click-verification required (a stronger trust bar than the self-serve path). Notifying a leader's *old* email address on change is deliberately deferred — this app has no outbound transactional email capability outside Supabase Auth's own magic-link template, and it isn't needed yet since `email`/`user_id` aren't privileged credentials until a later phase rewrites RLS to key off `user_id`.

### Data layer
All database access goes through service modules in `lib/services/`. Pages and components must not import `supabase` directly for CRUD operations.

- **`lib/services/campaignService.ts`** — all CRUD for the `campaigns` table: `getCampaignById`, `createCampaign`, `updateCampaign`, `deleteCampaign`, `getCampaignsByDateRange`, `findCampaign`, `getCampaignsForUser` (role-aware fetch + shared-leader merge for the main feed).
- **`lib/services/dropdownService.ts`** — `getPlacesForState` (returns `PlaceOption[]`, each with `place`/`site`/a combined `label` like "Orange 1"), `getLeadersForState`, `getLeaderMobile`. Single source of truth for dropdowns that appear on multiple pages.
- **`lib/services/placeService.ts`** — `addNewPlaceForState` — inserts into `state_places`; silently ignores duplicate (23505). Used when a user types a new place in a campaign form. Best-effort geocodes the place immediately via `lib/geocoding.ts`'s `geocodePlace()` (place name as the query, since this path has no separate `location` field the way `/admin/state-places` does), setting `location`/`latitude`/`longitude` on success — added 2026-09-17 after several places typed this way (Knoxfield among them) silently never got coordinates, invisible on the public Campaigns Near Me map with no warning (that map never geocodes on demand). A name Nominatim can't resolve is left ungeocoded exactly as before; `/admin/state-places` now flags any place missing coordinates regardless of cause.
- **`lib/services/stateLeadersService.ts`** — CRUD for the `state_leaders` table: `getStateLeaders`, `createStateLeader`, `updateStateLeader`, `deleteStateLeader`. Exports the `StateLeader` interface (includes `email`/`pending_email`/`email_confirmed_at`). `createStateLeader`/`updateStateLeader` best-effort log any `email` change to `state_leader_email_changes` (`source: 'admin'`; `updateStateLeader` only when the value actually changed) — never blocks the save if that logging write fails.
- **`lib/services/leaderVerificationService.ts`** — `findVerifiedStateLeaders(mobile, firstName)`, the server-side mobile+name matching logic shared by `/api/auth/validate-leader` and `/api/auth/propose-email` (extracted so the two routes can't drift apart on this security-sensitive check).
- **`lib/services/registrantEmailSuggestionService.ts`** — `getSuggestedEmailByMobile(mobile)`: a best-effort `registry.registrants` phone-match lookup powering the pre-fill suggestion on the leader email-capture step (`/login`); returns `null` on no match or any error, never throws.
- **`lib/services/statePlacesService.ts`** — CRUD for the `state_places` table: `getStatePlaces`, `createStatePlace`, `updateStatePlace`, `deleteStatePlace`. Exports the `StatePlace` interface (includes `site`; the table's uniqueness key is `state`+`place`+`site`).
- **`lib/services/backupService.ts`** — `exportBackup`/`restoreBackup` for `/admin/backup`. `BACKUP_TABLE_CONFIG` is the single source of truth for which tables are backed up and in what order — listed FK-safe (a table only ever appears after every table it references), since "replace" mode restore inserts top-to-bottom but deletes bottom-to-top.
- **`lib/services/campaignInterestService.ts`** — CRUD for the `campaign_interest` table: `getCampaignInterestList` (admin-wide, all rows newest-first, paired with each campaign's key details via a second query), `getCampaignInterestForLeader` (scoped to campaigns the given user leads, built on `campaignService.getCampaignsForUser` rather than re-implementing role/shared-leader filtering — same pattern as `trainingInterestService.getTrainingCampaigns`), `getCampaignInterestCounts` (per-campaign-id counts, powers the "N people interested" callout on `CampaignCard`), `setCampaignInterestContacted`. Exports the `CampaignInterest`/`CampaignInterestWithCampaign` interfaces. (Public submissions insert via the service role directly in `app/api/public/register-interest/route.ts`, not through this module — see that file.)
- **`lib/services/campaignInterestSmsService.ts`** — texts a campaign's owning leader (never a shared leader) via ClickSend when someone registers interest, called from both `app/api/public/register-interest/route.ts` and `app/api/public/campaigns-near-me/route.ts` right after their `campaign_interest` insert succeeds. `notifyLeadersOfCampaignInterest(client, { campaignIds, registrantFirstName, interestType })` takes an injected `SupabaseClient` (always `supabaseAdmin` in practice, since a leader's mobile isn't RLS-readable by an anonymous visitor) — same DI pattern as `backupService.ts`'s `exportBackup`, for testability with `supabaseMock.ts`. Groups the given campaign ids by owning `(state, leader)` and sends **one combined text per leader** even when several of their campaigns were ticked in the same submission, rather than one text per campaign. Never throws — a lookup/send/logging failure for one leader never blocks another or the registrant's own request; every outcome (`sent`/`failed`/`skipped_no_mobile`) is logged to `campaign_interest_sms_log`, learning from `registry.whatsapp_invite_log`'s own two-day silent-failure gap (see the Registry pipeline section) rather than only `console.error`-ing. Exports pure `toE164AuMobile`/`groupCampaignsByLeader`/`buildLeaderSmsMessage` helpers, fully unit tested without mocks.
- **`lib/services/trainingInterestService.ts`** — CRUD for the `training_interest` table: `getTrainingCampaigns` (training-category campaigns visible to the current user, built on `campaignService.getCampaignsForUser` rather than re-implementing role/shared-leader filtering), `getTrainingInterestCounts`, `getTrainingInterestForCampaign`, `setTrainingInterestContacted`. Also exports `TRAINING_CATEGORIES`/`isTrainingCategory()` — the single source of truth for which campaign category codes (`BOTJ`, `TLT`) count as a training session, used by both this service and the UI (`CampaignCard`, `InlineEditForm`) to decide when to show training-interest actions.
- **`lib/services/publicCampaignListService.ts`** — `getUpcomingFortnightDateStrings()` (cheap date-only helper, doubles as a cache key) + `fetchFortnightCampaignDays()` (the actual `campaigns`/`campaign_messages` query, shaped for `generateAndDownloadSlidesFromData`). Shared by `/api/public/temporary-upcoming-campaigns` and `/api/public/final-campaign-lists` — same fortnight/all-states dataset behind both pages, which differ only in copy and available actions.
- **`lib/services/campaignReportsService.ts`** — `getCampaignReportsNeedingReview(sinceDate)` (rows missing `derived_state`/`derived_place`/`derived_leader`, relies on `campaign_reports`' existing admin-only SELECT policy) + `updateCampaignReportDerivedFields(id, updates)` (writes via `POST /api/admin/campaign-reports/derived-fields` since the table has no authenticated write policy — service role only, for now). Powers `/admin/campaign-reports-cleanup`.
- **`lib/services/publicCampaignsNearMeService.ts`** — `buildNearbyMarkers()`: pure grouping/haversine-distance/radius-filter/sort logic for `/public/campaigns-near-me`'s map, given already-fetched campaign rows + cached `state_places` coordinates (no Supabase/geocoding calls of its own, so it's fully unit tested without mocks) — the admin equivalent's on-demand-geocoding step (`lib/services/nearbyCampaignsService.ts`/`campaignMapService.ts`) is deliberately not reused here, since that goes through the admin-only geocode-place API an anonymous visitor can't call. Shares `campaignMapService.ts`'s exported `placeKey()` so both stay consistent about matching a campaign's place against `state_places` by the same normalization.
- **`lib/services/publicUpcomingCampaignsService.ts`** — `buildUpcomingCampaignMarkers()`: pure grouping logic (no centre/radius, unlike `publicCampaignsNearMeService.ts`) for `/public/upcoming-campaigns`'s map, given already-fetched campaign rows + cached `state_places` coordinates only — same "no on-demand geocoding" rule as the near-me service, same reasoning. Backs `app/api/public/upcoming-campaigns/route.ts`.
- **`lib/placeSite.ts`** — `splitPlaceAndSite()`/`combinePlaceAndSite()`. The single source of truth for parsing/joining the numeric site suffix (e.g. "Orange 1" ⇄ `{ place: "Orange", site: "1" }`); used by the migration script and every place selector/display.
- **`lib/campaignLog.ts`** — fire-and-forget audit logging to `campaign_changes_log`. Skips automatically on admin routes and when logging is toggled off via `lib/appSettings.ts`. `fetchCampaignData` returns `Campaign | null`.

### Key shared libraries
| File | Purpose |
|------|---------|
| `lib/types.ts` | Shared TypeScript interfaces (`Campaign` and others) |
| `lib/constants.ts` | `AUSTRALIAN_STATES` array + `AustralianState` type; `DATABASE_TABLES` array used by the metrics dashboard |
| `lib/slideLayout.ts` | Slide/list formatting: state colors, date/time formatters, `getSlideDateHeadings()` |
| `lib/campaignRules.ts` | Rules engine — evaluates recurring campaign scheduling rules |
| `lib/campaignFilter.ts` | `getUserAdminStatusAndMobile()` — resolves the current user's admin level and state from `state_leaders` |
| `lib/errorUtils.ts` | `getErrorMessage()` — safe error-to-string coercion |
| `lib/clickSendClient.ts` | `sendSms(toE164, body)` — thin ClickSend REST adapter for the "text the leader" notification (`lib/services/campaignInterestSmsService.ts`). Requires `CLICKSEND_USERNAME`/`CLICKSEND_API_KEY`; always throws on failure (including missing config) rather than swallowing it, same as the registry pipeline's `EmailPort` |
| `lib/siteUrl.ts` | `getSiteUrl()` — resolves the app's own public origin (`NEXT_PUBLIC_SITE_URL` → Vercel's auto-injected production URL → localhost), used for links embedded in outbound messages |
| `lib/validation.ts` | `isValidMobile()`/`isValidEmail()` — loose format validators for public-facing contact-detail forms (currently `/public/register-interest`) |
| `lib/campaignReportParser.ts` | `parseTallyValue()`/`parseCampaignDate()`/`normalizeCampaignReportRow()` — parsing rules for the Campaign Report project's messy source data (`docs/campaign-report/BRIEF.md`), shared by the initial load and the later incremental catch-up import |
| `lib/ariseLayout.ts` | Canvas dimension constants (WIDTH=4200 px, HEIGHT=3000 px), font sizes, spacing constants, `AriseCampaign` interface, `apx()` helper, `simulateColumnCount()`, `computeColLayout()` |
| `lib/ariseCanvas.ts` | Canvas draw helpers (`drawBannerAndKey`, `drawDateHeader`, `drawCampaignLine`, `drawWeekSeparator`) and `renderAriseCanvas()` — the main rendering function |
| `lib/ariseGenerator.ts` | Public API only: `fetchCampaignsForDate()` (data fetching) + `generateAndDownloadAriseList()` (orchestrates render + JPEG download). Delegates drawing to `ariseCanvas.ts` |

### Component structure
- `components/MobileLayout.tsx` — shared shell (header, bottom nav) wrapping every page. Resolves admin status via `getUserAdminStatusAndMobile()`.
- `components/CampaignForm.tsx` — reusable add/edit form used by the admin panel; `CampaignData` interface includes a `category` field (e.g. `TWOL`, `BOTJ`, `TLT`).
- `components/ErrorBoundary.tsx` — global React error boundary.
- `components/CampaignMap.tsx` — shared Leaflet map for "every upcoming campaign, no radius", used by the public `/public/upcoming-campaigns` (previously the admin-only "View Campaign Map") and the admin `/admin/state-places-map`. Its marker/campaign types are minimal local interfaces (`CampaignMapPopupMarker`/`CampaignMapPopupCampaign` — id/date/time/leader, mirroring `NearbyCampaignsMap.tsx`'s own pattern below) rather than the admin-only `Campaign` type, so a public caller's leaner rows satisfy it without a cast. A `renderActions` prop (defaulting to the admin `MapPopupActions` stub) lets each screen swap in its own popup actions — `/public/upcoming-campaigns` passes `components/PublicMapRegisterInterestActions.tsx` instead, which — since a general public visitor has no known identity to look up (unlike `/public/campaigns-near-me` below) — captures first name + mobile/email in its own confirmation modal before POSTing to `/api/public/register-interest`.
- `components/NearbyCampaignsMap.tsx` — shared Leaflet map for "campaigns within a radius of a point", used by both `/admin/campaigns-near-me` and the public `/public/campaigns-near-me`. A `renderActions` prop (defaulting to the admin `MapPopupActions` stub) lets each screen swap in its own popup actions — the public screen passes `components/PublicCampaignInterestActions.tsx` instead, which shows the same consent-confirmation modal as Register Interest ("Proceed"/"Cancel") before actually recording interest into `campaign_interest`. That confirmation reuses `components/Modal.tsx`, which now portals to `document.body` rather than rendering in place — needed once a modal could be triggered from inside a Leaflet popup, since Leaflet's panes carry a CSS `transform` that would otherwise make a `position: fixed` modal position itself relative to the map instead of the screen. Its campaign markers are rendered noticeably larger/darker (30px, 0.5 fill-opacity) than `CampaignMap.tsx`'s own default (22px, 0.35) via `lib/leafletMarkerIcon.ts`'s `getStateMarkerIcon()` `{ size, fillOpacity }` options (2026-09-17, Peter's request — this map is meant to be tapped on a phone) — `getStateMarkerIcon`'s icon cache is keyed on those options too, not just state+place, so two callers requesting different sizes for the same place never collide.
- `components/registerInterest/` — `RegisterInterestClient.tsx` (+ `CampaignCheckboxList.tsx`, `InterestSummaryModal.tsx`), the Register Interest screen shared by `/public/register-interest` (current fortnight) and `/admin/register-interest` (admin-only, `nextPeriod` prop — the fortnight after). Moved out of `app/public/register-interest/` once a second route needed it.
- `components/CampaignInterestEntryList.tsx` — the registration-row list (campaign info, interest-type badge, Contacted checkbox) shared by `/admin/registered-interest` (admin-wide) and `/campaign-interest` (leader-scoped).
- `contexts/CampaignDatesContext.tsx` — shared date-range state for campaign views.
- `app/app/components/useCampaignForm.ts` — unified form hook shared by `CampaignCreateForm` and `InlineEditForm`. Owns all form state (date, state, place, time, leader, mobile, category, tl_ok, sr_ok), place creation, leader mobile auto-fill, and submit orchestration. `handleSubmit` accepts both `FormEvent` and `MouseEvent`.
- `app/app/components/useStateDropdowns.ts` — fetches and caches state-scoped place and leader lists. All `setState` calls are routed through Promise chains to satisfy the `react-hooks/set-state-in-effect` rule.

### Page map
| Route | Description |
|-------|-------------|
| `/login` | Mobile + name login. Also shows a skippable leader email-capture/confirm step (see "Leader email capture" under Auth & roles) before continuing to the usual post-login flow |
| `/confirm-email` | Landing page for the leader email-capture magic-link confirmation (see "Leader email capture" under Auth & roles). Standalone — doesn't affect what the leader is signed into |
| `/app` | Main campaign feed (home screen) |
| `/campaign-list` | Week-by-week slide-style campaign list |
| `/capture` | Geolocation capture for campaigns |
| `/record-results` | Record campaign results |
| `/results` | JPEG slide viewer |
| `/admin` | Admin panel hub |
| `/admin/state-leaders` | Manage leaders per state |
| `/admin/state-places` | Manage places per state |
| `/admin/campaign-rules` | Manage recurring campaign rules |
| `/admin/campaign-messages` | Manage per-date banner messages |
| `/admin/campaign-logs` | View audit log (paginated) |
| `/admin/generate-slides` | Generate JPEG slide ZIP for a Start/End Date + State range (defaults to the upcoming fortnight, all states) |
| `/admin/generate-report` | Generate campaign report for a Start/End Date + State range |
| `/admin/generate-week-campaigns` | "Single Week Campaigns" — produces the same landscape "A.F.J UPCOMING CAMPAIGNS" JPEG as the "Week 1 Campaigns" Admin Quick Action (`AdminQuickActions.tsx`), but for an explicit Start Date, End Date, and State chosen here instead of the quick action's fixed fortnight window |
| `/admin/leader-shares` | Leader share links |
| `/admin/campaigns-near-me` | Upcoming campaigns near the admin's current location |
| `/admin/register-interest` | Admin-only preview of the public Register Interest screen for the *next* fortnight (the two weeks starting right after the current period ends) — otherwise identical to `/public/register-interest`. Renders the same `RegisterInterestClient` with `nextPeriod`, which fetches `/api/public/register-interest?period=next` |
| `/admin/registered-interest` | List everyone who has registered interest via the public Register Interest link (per campaign), with a Contacted checkbox per registration. Not yet wired to leader-specific login/filtering — admin-only for now |
| `/admin/member-activity` | Active member counts (leader + team) by total, state, place, or campaign |
| `/admin/metrics` | Usage analytics, active users, and database row counts |
| `/admin/results-metrics` | Results dashboard — names recorded per category (TM/P/F/SP), by state, place, and campaign, for a date range |
| `/admin/campaign-categories` | Manage campaign categories (TWOL, BOTJ, TLT, …) |
| `/admin/campaign-reports-cleanup` | Campaign Report project: manually fill in State/Place/Leader for `campaign_reports` rows `lib/campaignReportMatcher.ts` couldn't confidently resolve on its own — see `docs/campaign-report/BRIEF.md` |
| `/admin/backup` | Export/restore a JSON snapshot of every admin-curated table (see `lib/services/backupService.ts`'s `BACKUP_TABLE_CONFIG` for the full, current list and restore ordering) |
| `/training-interest` | Leader-facing dashboard of the signed-in user's BOTJ/TLT training campaigns (own, shared, or — for admins — all), each with its public-link interest count and a Copy Link action. Visibility relies on `campaigns` RLS via `getTrainingCampaigns`, not an app-level admin check |
| `/training-interest/[campaignId]` | Full list of who registered interest in one training campaign via its public link, with a Contacted checkbox per registration. Access relies on `getCampaignById` returning `null` for a campaign the signed-in user isn't the owning/shared leader (or admin) of — same RLS-gated fetch the main feed uses |
| `/campaign-interest` | Leader-facing dashboard of everyone who has registered interest (via the public `/public/register-interest` link) in campaigns the signed-in user leads (own, shared, or — for admins — all). One flat list (unlike `/training-interest`'s per-campaign drill-down) since campaign interest shares a single public link rather than one per campaign. Visibility relies on `campaign_interest` RLS via `getCampaignInterestForLeader`. Reached via the "Manage my Campaign Interest" button on the post-login action chooser (shown only when this list is non-empty), or via an optional `?campaignId=` query param — filters the list to one campaign, linked from the "N people interested" callout on that campaign's card in the main feed (`CampaignCard`) |

### Public links (`/public/*`)
No-login pages, each registered in `lib/publicLinks.ts` (single source of truth for slug/title/description/path — drives both the page's own `<title>`/Open Graph metadata and the admin-managed list at `/admin/public-links`, where an admin can override a link's title/description without a redeploy). `middleware.ts`'s `PROTECTED_PREFIXES` doesn't include `/public`, so these pages are open by default; each is backed by a matching `/api/public/*` route using `supabaseAdmin` (service role) rather than the browser client, since anonymous visitors have no RLS access. Every `/api/public/*` route calls `enforceOrigin()` and a per-IP `createRateLimiter()` (`lib/corsUtils.ts` / `lib/rateLimit.ts`), and GETs are briefly in-memory cached since the response is identical for every caller.

`/public/training/[campaignId]` (below) is dynamic per-campaign, so it is deliberately **not** in `lib/publicLinks.ts` — that registry is for the fixed, enumerable links listed on `/admin/public-links`, not one entry per campaign. Its title/description are computed from the campaign record in its own `generateMetadata()` instead. Its GET is also not in-memory cached like the others, since each request is a single indexed-row lookup by campaign id rather than a table scan. `/public/campaigns-near-me` (below) is excluded from `lib/publicLinks.ts` for the same reason — personalized per registrant (`?r=<id>`), not a fixed link — and its GET is likewise not cached, since the response differs per registrant.

| Path | Description |
|------|-------------|
| `/public/week1-campaigns` | Always-current Week 1 Campaigns list, all states |
| `/public/temporary-upcoming-campaigns` | Leader-facing fortnight list — check/edit-redirect/download, distinct from Register Interest |
| `/public/final-campaign-lists` | Read-only version of the same fortnight list once leaders are done editing — Download only (no Edit button, no leader instructions). Same data source as `/public/temporary-upcoming-campaigns` (`lib/services/publicCampaignListService.ts`) |
| `/public/campaign-results` | Latest campaign results, all states, with per-page JPEG downloads |
| `/public/register-interest` | Tick upcoming campaigns and register interest ("Yes I'm In" / "Tell Me More"), capturing first name + a validated mobile number and/or email into `campaign_interest`. The only `/public/*` route with a POST (write) as well as a GET — see `app/api/public/register-interest/route.ts` |
| `/public/upcoming-campaigns` | Interactive map of upcoming campaigns, all states, with a This Week/Next Week toggle — the public replacement for the former admin-only "View Campaign Map" (`/admin/campaign-map`, removed). GET `app/api/public/upcoming-campaigns/route.ts` groups campaigns by place using only cached `state_places` coordinates (no on-demand geocoding — same rule as `/public/campaigns-near-me`). A "Show me Campaigns Near Me - my Postcode is" field geocodes a typed-in postcode via `app/api/public/geocode-postcode/route.ts` and recentres the map (no radius re-filtering, same as the admin map's old geolocation-based "Near Me"). Tapping a marker's "Yes I'm In"/"Tell Me More" (`components/PublicMapRegisterInterestActions.tsx`) collects first name + mobile/email inline before POSTing to `/api/public/register-interest`, since — unlike `/public/campaigns-near-me`'s registrant-specific link — there's no identity to look those up from |
| `/public/training/[campaignId]` | One training session's (campaign category BOTJ/TLT) details, with a form to register interest (name + mobile or email) into `training_interest`. Linked from the campaign's edit screen (`InlineEditForm`, category BOTJ/TLT) and from `/training-interest` via a Copy Link button. GET+POST both live in `app/api/public/training-interest/[campaignId]/route.ts` |
| `/public/campaigns-near-me` | Personalized "campaigns near you" map for one registry registrant, reached via `?r=<registry.registrants.id>` in their WhatsApp-invite email (`lib/registryPipeline/whatsappInvite.ts`) — never their raw name/email/mobile/postcode in the URL, just the opaque id. GET (`app/api/public/campaigns-near-me/route.ts`) centres the map on their postcode (state-level fallback if no postcode on file) for the 7 days starting today, within 60 km, using only cached `state_places` coordinates (no on-demand geocoding — that needs the admin-only geocode-place route). Tapping "Yes I'm In"/"Tell Me More" on a marker (`components/PublicCampaignInterestActions.tsx`) shows the same consent-confirmation modal as `/public/register-interest` before doing anything, then POSTs just `{registrantId, campaignId, interestType}` to the same route, which re-derives name/mobile/email server-side from `registry.registrants` and inserts into `campaign_interest` — no re-entering details already on file. `components/NearbyCampaignsMap.tsx` (shared with the admin `/admin/campaigns-near-me`) gained a `renderActions` prop so each screen can swap in its own popup actions instead of the admin's still-unwired `MapPopupActions` stub. |

### Database tables (key ones)
- `campaigns` — core records; `category` column is the campaign category flag (e.g. `TWOL`, `BOTJ`, `TLT`); `place`+`site` together identify the location
- `state_leaders` — leaders per state; `admin` column drives role ('AD' / 'SR' / null); `email`/`pending_email`/`user_id`/`email_confirmed_at` support the leader email-capture rollout (see "Leader email capture" under Auth & roles) — `email` has no DB-level uniqueness constraint (several rows for the same real person, e.g. an AD/SR/leader trio in one state, can share one address), and `user_id` isn't read by any RLS policy yet
- `state_leader_email_changes` — append-only audit trail of `state_leaders.email` changes (`source: 'self-serve' | 'admin'`, before/after value) — see `scripts/create_state_leader_email_changes_table.sql`. Excluded from `backupService.ts`'s `BACKUP_TABLE_CONFIG`, same reasoning as `campaign_changes_log`
- `state_places` — valid places per state; keyed on `state`+`place`+`site` (`site` holds a numeric sub-location suffix, e.g. "1" for "Orange 1")
- `campaign_rules` — recurring scheduling rules; also carries `place`+`site`
- `campaign_messages` — per-date banner messages
- `campaign_changes_log` — audit trail
- `results` — recorded result names per campaign; `category_code` is `'TM' | 'P' | 'F' | 'SP' | 'IR'` (Team Member / Partial Presentation / Full Presentation / Full Presentation + Sinner's Prayer / Information Request); `first_name` is free text, not a foreign key to `state_leaders`
- `campaign_interest` — members registering interest via the public `/public/register-interest` link; one row per (campaign, person), with `mobile` and/or `email` (at least one required — DB `CHECK` constraint, see `scripts/create_campaign_interest_table.sql` and `scripts/add_email_to_campaign_interest.sql`). `interest_type` is `'in' | 'more'` ("Yes I'm In" / "Tell Me More"); `contacted` + `contacted_at` track admin/leader follow-up. RLS lets an admin, or the campaign's owning/shared leader, read/update rows, via a join through `campaigns` mirroring that table's own SELECT/UPDATE policy — see `supabase/rls-policies.sql`. The public page inserts via the service role in its API route instead (anonymous visitors have no RLS access). `/api/public/campaigns-near-me` (see the Public links table) also inserts here the same way, for a registry registrant tapping "Yes I'm In"/"Tell Me More" on their personalized map — `first_name`/`mobile`/`email` are re-derived server-side from `registry.registrants`, never taken from the request body. Both insert paths also call `lib/services/campaignInterestSmsService.ts`'s `notifyLeadersOfCampaignInterest()` right after, best-effort texting the campaign's owning leader (see below)
- `campaign_interest_sms_log` — audit trail of every "text the leader" SMS attempt made by `notifyLeadersOfCampaignInterest()` (`scripts/create_campaign_interest_sms_log_table.sql`). One row per leader per submission (`campaign_ids` is an array — several ticked campaigns for the same leader are one combined text, one log row), `status` is `'sent' | 'failed' | 'skipped_no_mobile'`. Admin-only RLS `SELECT`; only the service role writes (bypasses RLS, no INSERT policy needed) — see `supabase/rls-policies.sql`. Excluded from `backupService.ts`'s `BACKUP_TABLE_CONFIG`, same reasoning as `campaign_changes_log` (a log table, not admin-curated content)
- `training_interest` — members registering interest in a training session via its per-campaign public link (`/public/training/[campaignId]`); one row per (campaign, person), with `mobile` and/or `email` (at least one required — DB `CHECK` constraint, see `scripts/create_training_interest_table.sql`). `contacted` + `contacted_at` track follow-up, same as `campaign_interest`, and RLS follows the identical owning/shared-leader-or-admin pattern — see `supabase/rls-policies.sql`
- `campaign_reports` — Campaign Report project (`docs/campaign-report/BRIEF.md`): aggregate per-campaign tallies (Partial/Full Presentations, Sinner's Prayer, Information Requests) from Jordan's Google Sheet, loaded via `scripts/campaign_reports_xlsx_to_json.py` + `scripts/import_campaign_reports.ts` (`lib/campaignReportParser.ts` owns the parsing rules, unit tested). `location_raw`/`leader_raw` are free text — no FK to `state_places`/`state_leaders`; `campaign_id` is a nullable, unenforced best-effort link for a future cleanup pass. Every tally/date column has a paired `*_raw` text column (a leader sometimes typed a note instead of a number/date) and `needs_review` flags rows the parser couldn't confidently parse. `submitted_at` (the sheet's own submission timestamp) is unique — the de-dup key for re-running the import against a newer export. Admin-only RLS for now — see `supabase/rls-policies.sql`. `derived_state`/`derived_place`/`derived_leader` (`scripts/add_derived_fields_to_campaign_reports.sql`) are populated only for rows submitted on/after 2026-05-06, via `lib/campaignReportMatcher.ts` matching `location_raw`/`leader_raw` against `state_places`/`state_leaders` (`scripts/derive_campaign_reports_fields.ts`) — null when not confidently resolvable, never guessed. See `docs/campaign-report/BRIEF.md`'s "Resumed with narrowed scope" section for why full sheet history isn't attempted

### Slide generation
The arise (Week 1 Campaigns) list generator is split across three modules:
- `lib/ariseLayout.ts` — constants, types, and column simulation (no canvas ops)
- `lib/ariseCanvas.ts` — all drawing primitives and `renderAriseCanvas()`
- `lib/ariseGenerator.ts` — data fetching and the public `generateAndDownloadAriseList()` entry point

Canvas is 4200×3000 px (14"×10" at 300 DPI). Column layout constants (`PLACE_COLS`, `TIME_COLS`, `LEADER_COLS`) control field widths; a dynamic `campaignScaleX` compresses text horizontally to fill the available width.

`app/admin/generate-slides/page.tsx` renders a separate JPEG slide (2250×3000 px, 300 DPI) using a different Canvas pipeline. `PLACE_COLS`, `TIME_COLS`, `LEADER_COLS`, `MOBILE_MAX_COLS` constants control field widths there.

### Registry pipeline (`docs/registry-pipeline/`)
A separate, isolated subsystem consolidating AFJ's ActiveCampaign registration data into this same Supabase project, plus a Supabase-Auth (magic-link) rollout for leaders — design in `docs/registry-pipeline/AFJ_PII_Technical_Implementation_Plan.md`, session scope in `docs/registry-pipeline/BRIEF.md`, deploy/run steps in `docs/registry-pipeline/OPERATIONS.md`. Unlike the rest of this app, it uses the Supabase CLI, Edge Functions, and `pg_cron` rather than Next.js API routes + Vercel Cron.

- **`staging` schema** — `staging.ac_events`: append-only, untransformed landing zone for raw AC contact payloads (`scripts/create_registry_pipeline_schema.sql`).
- **`registry` schema** — `registry.registrants` (one row per person, deduped on **email**, not AC contact ID — changed 2026-09-08, see `OPERATIONS.md`'s identity-redesign entry; `ac_contact_id` is nullable/informational only now, since the same person can plausibly hold more than one AC contact ID; email-less rows fall back to phone matching at the application level; also carries four fields added 2026-09-15 that turned out NOT to be simple AC custom fields despite looking like they should be — `webinar_session_at` (AC field `[24]` BOTJ Webinar Session, a datetime; NOT field `[23]` Rego Date, which has no CSV equivalent and stays unpromoted), `webinar_attended` (`'Yes'`/`'No'`/null, derived from AC tags `60`/`56` — tag `56` "Missed" is name-inferred and may not be in active use, so a live AC sync will likely only ever produce `'Yes'`/null), `code_of_conduct_agreed` (`'Yes'`/null only, `'Yes'` when AC tag `48` — the `/thewayoflife/` funnel tag — is present) and `code_of_conduct_agreed_at` (that tag's own `cdate`, not the contact's `registered_at`, which was confirmed to differ by months). See `lib/registryPipeline/tagDerivedFields.ts` and `scripts/add_webinar_and_code_fields_to_registrants.sql` for the full derivation and live-trace rationale. Historical (pre-2026-09 reload) registrants only get these four via the one-off `scripts/backfill_webinar_and_code_fields_from_csv.ts`), `registry.registration_events` (one row per form/list submission), `registry.twol_respondents` (`scripts/create_registry_twol_respondents_table.sql` — one row per AC List `[2]` `/wayoflife-responder/` submission: someone a TWOL presenter reported presenting "The Way Of Life" to, not a self-registration. Kept fully separate from `registry.registrants` — never counted as a registrant, never sent the WhatsApp invite email. Append-only, no dedup; see the table's own comment for the full root-cause writeup and the 2026-09-14 fix), `registry.known_source_tags` (AC tag ID → source label lookup, since AC List `[1]` is a catch-all for 3 of 4 registration funnels — tag, not list, is the real source identifier), `registry.sync_log` (audit trail of every ingestion run), `registry.registrant_edits` (`scripts/create_registry_registrant_edits_table.sql` — audit trail of manual corrections made from `/registry/manage`'s Edit mode: `registrant_id`, `field`, `old_value`/`new_value`, `edited_by`/`edited_by_email`; append-only, read back paginated by `/registry/manage/edit-log`), `registry.leader_roles` (`scripts/create_registry_leader_roles_table.sql` — maps `auth.uid()` to a role/state scope; `national_admin`/`whatsapp_admin` are high-privilege and MFA-gated via `registry.has_required_mfa()`), `registry.whatsapp_group_links` (`scripts/create_registry_whatsapp_group_links_table.sql` — `group_key`/`group_level`/`invite_url` for the WhatsApp-invite email below; only a single `'national'` row is used today, `group_level` kept generic for a possible future state/city breakdown), `registry.whatsapp_invite_log` (`scripts/create_registry_whatsapp_invite_log_table.sql` — one row per WhatsApp-invite email attempt: `status` is `'sent'` / `'failed'` (with `error`) / `'skipped_no_link'` (a qualifying registrant, but `whatsapp_group_links` had no `'national'` row yet), plus `resend_message_id` and `included_campaigns_near_me_link`; set up ahead of turning live sending on so none of those outcomes are only ever visible in the Edge Function's own console output — see the WhatsApp invite email bullet below and `/registry/manage/whatsapp-invite-log` in the Page map). No client ever queries these tables directly — only the service role (used exclusively by the Edge Function below) can; RLS is enabled with no policies (default-deny) plus an explicit `REVOKE` from `anon`/`authenticated`, except `leader_roles` where an authenticated leader may `SELECT` their own row only.
- **`supabase/functions/ac-sync`** — the scheduled polling Edge Function (Deno). Same function handles first-ever backfill and every incremental sync after (differs only by which timestamp it filters from). Thin adapters only (`acClient.ts` against the AC REST API, `db.ts` against `staging`/`registry` via the service role, `emailClient.ts` against Resend's HTTP API for the WhatsApp-invite email below) — all actual sync/transform logic lives in `lib/registryPipeline/` so it can be unit tested under Vitest without Deno. Scheduled via `pg_cron`+`pg_net` (`scripts/schedule_ac_sync_cron.sql`), every 4 hours (00:00/04:00/08:00/12:00/16:00/20:00 UTC) as of 2026-09-14 — was every 6 hours from 2026-09-13, and daily at 15:00 UTC before that, tightened each time so a new registration shows up in `/registry/manage` sooner (see `docs/registry-pipeline/OPERATIONS.md`'s "Cron frequency increased to every 6 hours" and "every 4 hours" entries). The `pg_cron` job is still named `ac-sync-daily` for continuity with its run history, despite no longer being literally daily. Original daily schedule confirmed live 2026-09-09 (see OPERATIONS.md's "Cron actually set up and confirmed live" entry) via a manual `net.http_post` test using the job's own call shape, not just `cron.job` showing a registered row.
- **`lib/registryPipeline/`** — framework-agnostic pipeline logic, fully unit tested (`__tests__/`): `sync.ts` (`runSync` — Section 6.1), `transform.ts` (`transformPendingStagingEvents` — Section 6.2; routes AC List `[2]` events to `DbPort.insertTwolRespondent()` instead of `upsertRegistrant()` — the one deliberate exception to source attribution being tag-based, not list-based, elsewhere in this pipeline, since List `[2]` has been repeatedly confirmed to be the sole use of that list, unlike List `[1]`'s genuine catch-all — see `scripts/create_registry_twol_respondents_table.sql`), `fieldMap.ts` (whitelist-only field mapping — enforces the field inclusion/exclusion decisions in plan Section 3.4; never add a field ID here without checking that section first; field `[24]` BOTJ Webinar Session is now promoted to `registry.registrants.webinar_session_at`, `[23]` Rego Date stays unpromoted), `phone.ts` (E.164 normalization), `sourceAttribution.ts` (tag-based source matching), `tagDerivedFields.ts` (registrant fields derived from AC tags rather than a custom field — `webinar_attended` from tags `60`/`56`, `code_of_conduct_agreed`/`_at` from tag `48`'s presence/`cdate` — deliberately checked as raw tag presence, not via `sourceAttribution.matchSourceTag`, whose first-match-wins behaviour is wrong for a multi-funnel contact), `listFilter.ts` (permanent List 3/5 exclusion + list-status active check), `rateLimiter.ts` (429 backoff + request pacing against AC's shared 5 req/sec limit), `ports.ts` (the `AcPort`/`DbPort`/`EmailPort` interfaces that decouple this logic from the Deno/Supabase/Resend adapters — `DbPort.upsertRegistrant` returns `isNew` so callers can tell a genuine first-time INSERT from an existing row just being updated, which `whatsappInvite.ts` below depends on; `EmailPort.sendWhatsAppInviteEmail` always throws on failure — including a missing `RESEND_API_KEY`, which the adapter used to just log and swallow — resolving instead with the Resend message id and whether the Campaigns-Near-Me link was included, both persisted by `DbPort.logWhatsAppInviteAttempt` to `registry.whatsapp_invite_log`), `whatsappInvite.ts` (`shouldSendWhatsAppInvite`/`buildWhatsAppInviteEmail` — the WhatsApp-invite email's send-or-not decision and content, invoked from `transform.ts` right after `upsertRegistrant`; every outcome — sent, failed, or skipped because no invite link is configured yet — is logged via `DbPort.logWhatsAppInviteAttempt`, itself wrapped in its own try/catch so a logging failure can't affect the underlying staging event either; a failed send never marks the underlying staging event as an error, since the registrant/registration-event rows are already committed by that point; `buildWhatsAppInviteEmail`'s third argument is a personalized `/public/campaigns-near-me` link — built by `ac-sync/emailClient.ts` from the registrant's id plus `NEXT_PUBLIC_SITE_URL`, omitted from the email entirely when that env var isn't set in the Edge Function's environment, rather than shipping a broken/localhost link), `mfaGate.ts` (`evaluateMfaGate` — pure routing decision for the `/registry` portal below; `isNationalRegistryAdmin()` — single source of truth for "is this role `national_admin`/`whatsapp_admin` vs. a state-scoped `state_leader`", used by both the Manage button's visibility and `/api/registry/manage-summary`'s server-side check), `csvParse.ts` + `csvRegistrantTransform.ts` (the registrations-reload rules, including the four webinar/Code-of-Conduct fields added 2026-09-15 — `parseAuDateTime()` for the CSV's "Webinar" column, which carries a time-of-day unlike every other date column here; `parseYesNo()` for "W/Done"/"Code" — see `scripts/reload_registrants_from_csv.ts` below), `registrantCounts.ts` (pure date-window + per-state tallying for `/registry/manage`'s console — `countAllRegistrants`/`countRegistrantsForPeriod` for the grid, `filterRegistrantsForCell` for its click-to-drill-down record list, all run client-side against one fetched registrant list so both the filter dropdowns and record pane are instant), `registrantValidation.ts` (`EDITABLE_REGISTRANT_FIELDS`/`EDITABLE_FIELD_COLUMNS` — the whitelist of which `registry.registrants` fields `/registry/manage`'s Edit mode may touch, deliberately excluding email/phone/registered_at; `isValidAustralianPostcode`/`isValidRegistrantState` — shared by the page's inline inputs and `/api/registry/manage-record`'s server-side re-validation; `EDITABLE_FIELD_LABELS` — display label for a `registry.registrant_edits.field` value, used by `/registry/manage/edit-log`), `registrantLookupFilters.ts` (`getLookupOptions`/`filterByLookups` — the record pane's five value-lookup dropdowns: options are the distinct values already present among the selected cell's records, deliberately excluding State/Date registered per Peter's request), `leaderMatch.ts` (`matchRegistrantsToLeaders` — cross-references registry.registrants against `public.state_leaders` by normalized phone, since state_leaders has no email column to match on; used by `/api/registry/manage-summary` to tag each registrant `isLeader`/`leaderName` — deliberately no state, just whether they're a leader at all — computed live on every request rather than synced/stored), `manageSummaryTypes.ts` (response shapes shared between the Manage routes and their pages), `registrantNameGuard.ts` (`protectExistingRegistrantName()` — called from `db.ts`'s `upsertRegistrant` on every update against an existing row; pins `first_name`/`last_name` back to their stored value once non-blank, so a later sync under the same email/phone can never silently replace a known name — see the "a later sync can no longer silently overwrite a registrant's name" follow-up below).
- **`/registry` portal** — real Supabase Auth (magic-link email) + MFA sign-in for `registry.leader_roles`, independent of the main app's mobile+name login (see `lib/auth.ts`) and deliberately not nested under `/admin`, since a `national_admin`/`whatsapp_admin` doesn't need to be a `state_leaders` row at all. `lib/registrySupabaseClient.ts` is a second `supabase-js` client with its own `localStorage` key so the two sessions can coexist on one browser/device. Flow: `/registry/login` (invite-only — `shouldCreateUser: false`) → magic link → `/registry/auth/callback` (client-side code exchange — must run in-browser, not a server Route Handler, so the session has somewhere to persist to) → `lib/registryAuth.ts`'s `getRegistryAccessState()` reads the caller's own `leader_roles` row + current AAL/factors and routes via `evaluateMfaGate()` to `/registry/mfa/enroll` (first-time TOTP), `/registry/mfa/challenge` (returning user), `/registry/no-access` (valid sign-in, no role), or `/registry` (landing page — links to `/registry/manage` for a `national_admin`/`whatsapp_admin`; a plain `state_leader` currently has nothing to click through to from there, since the only other screen, Recent Registrations, was retired — see below). `middleware.ts` guards `/registry/*` with two cookies (`registry_auth` once a session exists, `registry_session` once the MFA gate clears) — a UX guard only, same as `app_session`; the real boundary is RLS + `registry.has_required_mfa()`. Access is seeded via `scripts/seed_registry_leader_roles.ts` (creates the `auth.users` row via the Admin API if needed, then upserts `leader_roles`) — also requires `scripts/grant_registry_authenticated_schema_usage.sql` (schema `USAGE` for `authenticated`, the same class of gap `grant_registry_pipeline_service_role.sql` fixed for `service_role`). See OPERATIONS.md's 2026-09-05 section for full detail and its "not yet verified against a live Supabase project" caveat.
- **`/registry/recent-registrations`** — retired 2026-09-14. Was a temporary reconciliation view (a live, read-only, per-contact AC lookup, nothing persisted) for registrants created since 2026-08-22, built before the registrations reload shipped. Kept around afterward as a "harmless" spot-check tool (see OPERATIONS.md's 2026-09-08/09 entry), but its per-contact walk against a fixed cutoff only ever grew, and it started reliably exceeding the route's 60s `maxDuration` — see OPERATIONS.md's 2026-09-14 entry. `/registry/manage` (DB-backed, no live AC calls, no timeout risk) already covered everything this showed, so it was removed rather than patched.
- **`/registry/manage`** — `national_admin`/`whatsapp_admin`-only console (its landing-page button is hidden from a `state_leader`, and `/api/registry/manage-summary`/`manage-record` independently reject one server-side): shows the most recent `registry.sync_log` row's timestamp/status/counts ("when did the cron job last run"), then an interactive grid comparing registrant counts — an unfiltered "All AFJ Registrations" row plus a "Primary Filter"/"Alternative Filter" row each with its own period dropdown (last 24 hours … a fixed calendar year) — broken down by state, with the state columns shaded using `lib/slideLayout.ts`'s `getSlideStateShade()` (shared with the campaign results slides, now taking an `alpha` param). Clicking any non-zero count opens a record pane underneath listing the actual matching registrants, and boosts that one cell's shading intensity so it's clear which count the list below it belongs to; the pane stays pinned to the clicked cell's row+column identity, so if that row's own period dropdown changes afterward the list re-filters live rather than going stale. Above the table, five lookup fields — one per field except State and Date registered — let a national admin narrow the pane to an exact value on First name/Last name/Email/Mobile/Postcode; each is a type-to-filter combobox (`LookupSelect` in `page.tsx`) rather than a plain `<select>`, since a leader's full value list is long enough to make scrolling tedious — typing narrows a floating options list by case-insensitive substring, Enter picks the top match, and a click or Escape also closes it. Each field's own option list is exactly the distinct values already present among that cell's records (plus a "(blank)" option when at least one record has none), computed by `lib/registryPipeline/registrantLookupFilters.ts` and reset whenever a different cell is selected. A View/Edit toggle in the pane switches First name/Last name/State/Postcode between plain text and inline-editable cells (State a dropdown of `AUSTRALIAN_STATES`, Postcode format-validated as 4 digits — `lib/registryPipeline/registrantValidation.ts`); Email/Mobile/Date registered are never editable there, since email/phone are the pipeline's own identity/dedup keys, and neither are the four webinar/Code-of-Conduct columns added 2026-09-15 (synced/derived from AC, not hand-corrected — see the `registry` schema bullet above). Each field saves independently via `PATCH /api/registry/manage-record` (whitelisted + re-validated server-side, written via the service role since `registry.registrants` has no authenticated write policy) and is logged to `registry.registrant_edits`; a successful save patches the in-memory registrant list directly, so the grid's counts and the pane's own contents both re-tally live (e.g. correcting a state moves that person to the right column immediately). The summary API returns every registrant's full record (name/email/phone/state/postcode/registered_at), plus a live `isLeader`/`leaderName` cross-reference against `public.state_leaders` by normalized phone (`lib/registryPipeline/leaderMatch.ts` — the pipeline's first read of the main app's `public` schema, made possible since both live in the same Postgres database, just different schemas), shown as a small icon next to a matched registrant's last name (hover for the matched leader's name; no state shown — Peter only needs the yes/no), in one paginated fetch; all tallying and drill-down filtering (`lib/registryPipeline/registrantCounts.ts`) runs client-side against that one fetch. After "Registered" (date only, no time — `formatDateOnly()`, distinct from the cron-run timestamp elsewhere on the page which still shows both), the pane's last four columns are the webinar/Code-of-Conduct fields added 2026-09-15 — Webinar session (date+time, `formatDateTime()`), Webinar attended, Code agreed, Date agreed — read-only, not sortable, and not among the five lookup fields above (all three could be added later without a schema change). The First name/Last name/Postcode/Registered headers are sortable (click to toggle ascending/descending, ▲/▼ indicator — Registered defaults to descending/newest-first, shown from the start) — Email/Mobile/State aren't, per Peter's request; this sort is local view state in `RecordsPane`, not reset when a different cell is selected (unlike the mode toggle and lookup filters), since a sort preference stays meaningful regardless of which cell's records it's applied to.
- **`/registry/manage/edit-log`** — reached via a "View Edit Log" link on `/registry/manage`; same `national_admin`/`whatsapp_admin`-only gate. Paginated (50/page, server-side via `/api/registry/manage-edit-log`, newest first), listing every row of `registry.registrant_edits` joined with the registrant it was about (name/email, plus state for the same per-state row shading as Manage) — edited-at, registrant, field (labeled via `EDITABLE_FIELD_LABELS`), old value, new value, and who made the edit.
- **`/registry/manage/whatsapp-invite-log`** — same pattern as `/registry/manage/edit-log` above (reached via a "View WhatsApp Invite Log" link on `/registry/manage`, same gate, same 50/page/newest-first pagination, this time via `/api/registry/manage-whatsapp-invite-log`), listing every row of `registry.whatsapp_invite_log` joined with the registrant it was for — attempted-at, registrant, status (`Sent`/`Failed`/`Skipped (no invite link configured)`, color-coded), a detail column (the error for a failure, the Resend message id for a send), and whether the Campaigns-Near-Me link section was included.
- **Registrations reload from Lorraine's spreadsheet** — done (2026-09-08/09). `registry.registrants` was fully replaced from a CSV export (9,128 rows, deduped/cleaned via `lib/registryPipeline/{csvParse,csvRegistrantTransform}.ts`, both unit tested); `ac_contact_id` is now nullable/informational, email is the identity key. `ac-sync` redeployed with the matching `upsertRegistrant()` change. See `OPERATIONS.md`'s "run, verified, done" entry for the two real issues hit during execution (a multi-table `TRUNCATE` FK rule, and a Podman+SELinux container bind-mount issue on Fedora) — both fixed live, not worked around.
- **Webinar/Code-of-Conduct fields added, 2026-09-15** — `reload_registrants_from_csv.ts` and `csvRegistrantTransform.ts` now also read the CSV's Webinar/W-Done/Code/Date Agreed columns, originally scoped out of the reload above as workflow-tracking columns with no clear need. `scripts/backfill_webinar_and_code_fields_from_csv.ts` is the incremental (UPDATE-only, matched by email, never overwrites) counterpart for the ~9,128 registrants already loaded before this need emerged — `reload_registrants_from_csv.ts` itself stays a full truncate+reload tool and refuses to run against a non-empty table. See the `registry` schema bullet above for what these fields actually turned out to mean on the AC side.
- **WhatsApp invite email (single national group)** — email content built 2026-09-13, not yet sending live (Peter is still shaping the email's content — see the "Campaigns Near Me" bullet below — before adding the real invite link row that would let it start). `AFJ_PII_Technical_Implementation_Plan.md` Section 8's original state/city invite-link design was blocked on a leadership decision never resolved (AFJ's real footprint, ~100 groups/9,000+ subscribers, exceeds WhatsApp's published "Communities" feature limits — see BRIEF.md/the executive report). Peter's call: skip the Communities feature and the platform decision entirely, and start with one plain, ordinary WhatsApp group invite link (no Communities-feature scale limit applies to that) for the whole country. On every sync, right after `upsertRegistrant()`, `transform.ts` checks `shouldSendWhatsAppInvite()` (genuinely new row — not an existing registrant being re-synced — with a usable email) and, if `registry.whatsapp_group_links`' `'national'` row has a link configured, sends it via `ac-sync/emailClient.ts` (Resend's HTTP API, reusing the Resend account already used for this project's Supabase Auth magic-link emails, but a separate `RESEND_API_KEY` Edge Function secret since this calls Resend directly rather than through Supabase Auth). A failed send is logged and never fails the sync or gets retried (retrying would just re-upsert the same now-existing registrant forever, never able to send since `isNew` would be false every time after). Requires the SQL migration run, a real invite URL inserted into `registry.whatsapp_group_links`, `RESEND_API_KEY` set, `NEXT_PUBLIC_SITE_URL` set (for the Campaigns Near Me link below — the email still sends without it, just without that section), and `ac-sync` redeployed — nothing sends until the link/key steps are done (missing link or missing key both fail safe: log-and-skip, not an error).
- **"Campaigns Near Me" link in the invite email** — done 2026-09-13, see `/public/campaigns-near-me` in the Public links table above and `lib/services/publicCampaignsNearMeService.ts` above. Added as a second section of the same invite email (`buildWhatsAppInviteEmail`'s `campaignsNearMeUrl` param) so a registrant can see and act on nearby campaigns immediately, independent of whether/when they actually join WhatsApp.
- **Deliberately not yet built**: state/city-level WhatsApp invite links (would need the platform/scale decision above, if ever revisited), and the RLS-scoped `registry.v_leader_registrants` view / export RPC (the auth rollout above unblocks this — still not built).
- **TWOL responder records wrongly synced into `registry.registrants`** — fixed 2026-09-14. Investigating a missing-postcode report (a 12 Sept 2026 registrant with no postcode) traced back to AC List `[2]` (`/wayoflife-responder/`) submissions — a TWOL presenter reporting on someone they presented to, not a self-registration, and a form with no Postcode field at all. These were being upserted into `registry.registrants` like any genuine registrant, which also meant they were eligible for the WhatsApp invite email once that goes live. Fixed by adding `registry.twol_respondents` and routing List `[2]` there in `transform.ts` (see the two bullets above). `scripts/migrate_wayoflife_responders_to_twol_respondents.ts` moved the 46 already-existing registrants whose *only* registration_events were List `[2]` (backed up to `backups/` first); registrants who also had a genuine List-1 event were left in place, since they really did register themselves at some point.
- **Follow-up: full AC tag audit, 2026-09-14** — same investigation, one level deeper: an audit of every AC tag appearing anywhere in `staging.ac_events` (57 distinct tags) found two more populations with no genuine registration-funnel signal behind them, previously flowing straight into `registrants` (or `twol_respondents`, for one of them) — `lib/registryPipeline/tagExclusion.ts`'s `EXCLUDED_SOURCE_TAG_IDS` extended from `['11']` to also cover `[40]`/`[41]` ("Mobilise - Make a Donation: Form completed" — financial-intent-adjacent, same sensitivity category as the already-excluded `[12]`/`[13]` fields and List 5), `[8]`/`[9]` ("TWOL Explore More: Requested" — near-blank contacts), and `[6]` ("TWOL Video: Requested" — the tag on the very record, Lorraine's, that triggered this audit; excluded entirely rather than kept in `twol_respondents`, since a video-request click isn't the same kind of event as a presenter's response report). The exclusion check in `transform.ts` was also reordered to run *before* the List `[2]` routing decision, not after — a would-be-excluded contact must be excluded regardless of which list their event lands on. `scripts/cleanup_excluded_tag_only_records.ts` removed the small number of already-synced rows this affected (1 registrant, 3 `twol_respondents`, backed up first) by re-checking every contact's full tag history in `staging.ac_events`, not just their most recent sync.
- **Follow-up: unsubscribe tracking, 2026-09-14** — investigating whether unsubscribed contacts could flow into the registry found no live leak (`isActiveListStatus()` already rejects any non-`'1'` list status before a contact ever becomes a registrant — confirmed live: 18,331 of 63,835 staging events, 29%, correctly skipped) and confirmed AC's "Unsubscribes" was never a distinct source to begin with (plan Section 3.3: "Per-contact/per-list status, not a distinct source"). It did surface one real gap, now fixed: an *existing* registrant who later unsubscribed in AC had their status change silently skipped with no update to their stored row — `transform.ts` now calls the new `DbPort.markRegistrantUnsubscribedByEmail()` in that case, so `registry.registrants.unsubscribed` stays live instead of only reflecting a one-time CSV snapshot. Separately, Jordan's own "Unsubscribes" tracking tab (`AFJ Tracking Export`, distinct from Lorraine's spreadsheet that originally seeded this column) had never been cross-checked against the registry — `scripts/jordan_unsubscribes_xlsx_to_json.py` + `scripts/mark_unsubscribed_from_jordan_sheet.ts` did a one-off match-and-flag: 540 registrants (of 2,799 email matches; 2,259 were already flagged from Lorraine's data) updated to `unsubscribed = 'Yes'`, backed up first.
- **Follow-up: explicit unsubscribed check on the WhatsApp invite gate, 2026-09-14** — `shouldSendWhatsAppInvite()` (`whatsappInvite.ts`) previously relied solely on `isNew`/`email`; for anyone already flagged `unsubscribed = 'Yes'` that happened to be a correct guarantee (they already have a row, so `isNew` is always `false` on any later sync) but an emergent one, not something the invite gate actually checked. Made it explicit: `DbPort.upsertRegistrant` now also returns the matched row's pre-existing `unsubscribed` value (`null` for a genuine new insert), and `shouldSendWhatsAppInvite` checks it directly. **Known limitation** (documented, not fixed): this doesn't protect a genuinely first-time contact who is AC-active but happens to be on Jordan's separately-maintained Unsubscribes sheet — that sheet isn't a live, queryable source for `ac-sync`; see `OPERATIONS.md`'s entry for the full detail on why and what closing it properly would take.
- **WhatsApp invite email logging, 2026-09-15** — added `registry.whatsapp_invite_log` (see the `registry` schema bullet above) ahead of turning live sending on, so a misconfiguration doesn't fail silently: every attempt `transform.ts` makes is now persisted, not just `console.error`'d inside the Edge Function. `EmailPort.sendWhatsAppInviteEmail` (`ports.ts`) was changed to always throw on failure — including a missing `RESEND_API_KEY`, previously swallowed with a log-and-return — so `transform.ts` has exactly one path (its existing try/catch around the send) that logs every outcome. Requires the SQL migration (`scripts/create_registry_whatsapp_invite_log_table.sql`) run before `ac-sync` is redeployed with this change. Followed immediately by `/registry/manage/whatsapp-invite-log` (same day) — a paginated viewer for this table, mirroring `/registry/manage/edit-log`'s pattern exactly (same gate, same `/api/registry/manage-*` route shape, same 50/page/newest-first), reached via a new "View WhatsApp Invite Log" link on `/registry/manage`.
- **Follow-up: the invite log itself was silently failing to write, 2026-09-17** — two days after the above shipped and the real invite link was configured, Peter reported new registrations with nothing appearing in `/registry/manage/whatsapp-invite-log`. Root cause: `whatsapp_invite_log`'s `id` column was declared `BIGSERIAL`, unlike every other table in this schema (`ac_events`/`registration_events`/`sync_log`, all `GENERATED ALWAYS AS IDENTITY`) — a `SERIAL` column's backing sequence is a separate grantable object, not covered by `service_role`'s table-level grants the way an `IDENTITY` column's is (see `grant_registry_pipeline_service_role.sql`). Every insert had been failing with `permission denied for sequence ..._id_seq` since the table was created, caught by `transform.ts`'s own console-error-only safety net around the log write — so the underlying WhatsApp invite sends were unaffected (the send happens before the log write), but literally zero rows had ever been logged. Fixed via `scripts/fix_whatsapp_invite_log_sequence_grant.sql` (grants the existing sequence + closes the general gap with `ALTER DEFAULT PRIVILEGES ... ON SEQUENCES`, which the original grant script only ever covered for tables); `create_registry_whatsapp_invite_log_table.sql`'s column definition corrected to match convention for any future fresh environment. Diagnosed and reproduced with `scripts/debug_check_whatsapp_invite_gap.ts` + `debug_test_invite_log_write.ts`.
- **Manual backfill for pre-link registrants, 2026-09-17** — the two registrants processed during the sequence-grant-bug window (Esther Wong, sathwik valla) were confirmed via Resend's own dashboard to have actually received the invite despite the missing log row, so `scripts/backfill_log_confirmed_sent_pre_fix.ts` retroactively logged them as `sent` (one-off, not re-runnable against new cases — it's a hardcoded list of the two confirmed emails). Separately, Peter asked about registrants who registered *before* the national link was ever configured (e.g. Nimmy Joy, 13 Sept) and so were never even candidates for the automated send — sizing that population found ~6,301 registrants with an email who have never received this invite, going back to 2017 (`scripts/debug_count_unsent_invite_candidates.ts`), which is a full historical mass-send decision on its own, not something to do as a side effect of a bug fix. Peter's call: build a manual backfill tool scoped to a configurable recent window (default last 14 days by `registered_at`, override with `--since=YYYY-MM-DD`) rather than the full historical population. `scripts/backfill_whatsapp_invite_manual.ts` — dry-run by default (lists candidates only), `--apply` (with `RESEND_API_KEY` passed inline, never via `.env.local`) to actually send; reuses `whatsappInvite.ts`'s exact email content and logs every attempt to `registry.whatsapp_invite_log` (`raw_staging_id: null`, since these aren't tied to a sync) so they show up in `/registry/manage/whatsapp-invite-log` like any other attempt; a registrant already logged `sent` is automatically excluded, making the script safe to re-run. Caught one bug of its own before ever running for real: filtering "not unsubscribed" via `.neq('unsubscribed', 'Yes')` silently excluded every `unsubscribed: null` registrant too (Postgres's three-valued NULL logic — `NULL <> 'Yes'` is `NULL`, not `true`), returning zero candidates including Nimmy; fixed by filtering that condition in JS instead of in the PostgREST query.
- **Email copy tweak, 2026-09-17** — `buildWhatsAppInviteEmail()` (`whatsappInvite.ts`) now adds one more line right after the Campaigns Near Me link (both HTML and plain-text parts, only when that section is present at all): "Keep this link safe and click on it anytime to get an up-to-date view of campaigns near you, and register your interest in joining one." — encourages treating it as a standing bookmark rather than a one-time click. The standalone "WhatsApp Invite Preview" artifact (a rendered mock-up of this email, shared with Peter separately — not checked into this repo) was updated to match.
- **Follow-up: a later sync can no longer silently overwrite a registrant's name, 2026-09-17** — investigating a "search can't find Lorraine Walker" report traced to her own registrant row having `first_name`/`last_name` of "Test"/"Testing", from a test registration she'd made with her real email address. Raising it with her ruled out any fix based on detecting "test-looking" data: a shared email genuinely can belong to more than one real person (a couple registering under one household email), and team leaders genuinely do run test submissions — there's no reliable way to tell a test apart from a real (re-)registration by its content alone. Fixed at the point of overwrite instead: `db.ts`'s `upsertRegistrant` now runs every update through `lib/registryPipeline/registrantNameGuard.ts`'s `protectExistingRegistrantName()` — once a registrant's `first_name`/`last_name` is non-blank, no later sync under the same email/phone can replace it (a currently-blank name can still be filled in). A genuine name correction now only happens through the existing audited Edit mode on `/registry/manage`. Scoped to name only, since that's the field this incident was about — `state`/`postcode`/`phone`/etc. still get overwritten by each sync as before. **Not fixed by this change**: Lorraine's own row still reads "Test"/"Testing" — that's a data correction (via `/registry/manage`'s Edit mode), separate from the sync behavior fix.
- **Follow-up: audit for other affected registrants + data correction, 2026-09-17** — same day, checked how far the above went by diffing every live `registry.registrants` row against the original CSV reload baseline (`/home/peterv/Documents/AFJ Registrations.csv`, re-run through `csvRegistrantTransform.ts` exactly as the reload did) and against the full `staging.ac_events` history (63,854 rows). Findings: **4 other registrants** (besides Lorraine) had a CSV-verified real name replaced by an obvious test/placeholder value the same way — David Chellappa, Peter Southam, Sabrina Tassone, and Peter (Viertmann) himself; **zero** emails were found shared by two genuinely distinct AC contact IDs (the literal "couple, two AC records" case hasn't manifested in the data, though two lower-confidence mismatches — `arunmavai@gmail.com`, `efunkay@hotmail.com` — look like a different household member may have overwritten the name under one shared AC contact; left alone pending a closer look); and the 32 CSV-baseline emails with no live registrant row at all are **fully accounted for** by the already-documented 2026-09-14 TWOL-responder migration (moved to `registry.twol_respondents`, not lost) — no evidence of any registration silently dropped by a same-email collision. `scripts/fix_test_data_overwritten_names.ts` (dry-run by default, `--apply` to write; backs up to `backups/` first, re-verifies each row's current state before touching it) restored the 5 confirmed names and logged one `registry.registrant_edits` row per field changed, attributed to the admin who authorized the fix — same audit trail a manual Edit-mode save produces.
